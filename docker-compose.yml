version: "3.9"

services:
  # TODO: Add production build for BE && FE

  redisGraph:
    image: "redislabs/redisgraph:2.8.13"
    ports:
      - "6379:6379"
    volumes:
      - "./db-data/redisGraph:/data"
